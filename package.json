{
  "devDependencies": {
    "typescript": "^5.9.3",
    "concurrently": "^8.0.1"
  },
  "dependencies": {
    "axios": "^1.13.2"
  }
  ,
  "scripts": {
    "dev:all:concurrent": "concurrently \"cd backend && npm run dev\" \"cd ml-services && python -m uvicorn app.main:app --reload --port 8000\" \"cd frontend && npm run dev\"",
    "dev:all:ps": "powershell -ExecutionPolicy Bypass -File scripts/start-all.ps1",
    "dev:all": "npm run dev:all:concurrent",
    "build": "cd backend && npm run build && cd ../frontend && npm run build"
  }

}
