# Fine-tuning

This folder contains scripts and instructions for fine-tuning the model using LoRA/QLoRA and for embedding model fine-tuning.

Note: This is a placeholder and requires GPU resources and authenticated API access to fine-tune models.

- `run_lora.sh` - Script to run LoRA adaption (placeholder)
- `run_qlora.sh` - Script to run QLoRA (placeholder)
- `data/` - place to store training data in the required format
